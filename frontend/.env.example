# Vercel sets VITE_STATIC_API=true (CDN bundle — fast, no serverless API)
# VITE_STATIC_API=true

# Optional: external API URL when frontend is hosted separately from the API
# VITE_API_BASE=https://your-api.onrender.com
